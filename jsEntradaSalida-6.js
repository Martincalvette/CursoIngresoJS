/*Debemos lograr tomar Los numeros por ID ,
transformarlos a enteros (parseInt) y Sumarlos.
mostrar el resulto por medio de "ALERT"*/
function sumar()
{
	var valorA;
	var valorB;
	var resultado;

		valorA = document.getElementById('numeroUno').value;
		valorB = document.getElementById('numeroDos').value;

			var resultado = parseInt(numeroUno) + (numeroDos);

			alert ("la suma es" resultado );

			//jsEntradaSalida-6.js:15 Uncaught SyntaxError: missing ) after argument list

			
}

